meta {
  name: forgot-password
  type: http
  seq: 8
}

post {
  url: {{BASE_URL}}/auth/forgot-password
  body: json
  auth: inherit
}

body:json {
  {
    "email": "john.doe@example.com"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
