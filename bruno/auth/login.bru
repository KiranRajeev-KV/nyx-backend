meta {
  name: login
  type: http
  seq: 3
}

post {
  url: {{BASE_URL}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "john.doe@example.com",
    "password": "StrongPassword123"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
