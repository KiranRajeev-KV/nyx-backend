meta {
  name: login
  type: http
  seq: 4
}

post {
  url: {{BASE_URL}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "john@example.com",
    "password": "NewStrongPassword123"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
