meta {
  name: login
  type: http
  seq: 3
}

post {
  url: {{BASE_URL}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "john@example.com",
    "password": "password123"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
